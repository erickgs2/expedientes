# Native build outputs live inside android/.gitignore and ios/.gitignore
# (generated by Capacitor).

# Written by capacitor.config.ts at sync time from EXPEDIENTES_SERVER_URL;
# env-dependent, must not be committed.
www/server-url.js
